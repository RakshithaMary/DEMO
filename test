Python Runtime Details

Python Version: 3.10.x (minimum)

Runtime: CPython

Framework: FastAPI

Server: Uvicorn (ASGI)

Environment

Python virtual environment (venv)

Dependencies installed via pip using requirements.txt

Startup Command
uvicorn app.main:app --host 0.0.0.0 --port 8000

Key Packages

fastapi

uvicorn

neo4j

pydantic

python-dotenv

Configuration

Runtime configuration via environment variables

No credentials hardcoded in source code

TLS-enabled Neo4j connection
