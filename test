I need you to update the existing client sampling script using the latest canonical data. Do not rebuild the logic from scratch unless necessary. Use Isaac’s existing script as the starting point, but replace any older source datasets with the latest canonical datasets I provide.

The sampling must be done at the **parent client level**, not legal-entity level.

Please implement the logic in the following order:

1. **Start from the latest client population**

   * Use the latest/current client record, not an old month-end classification.
   * For each parent client, use the most recent LOB/market classification available.
   * Do not recreate connection rollups if the canonical/client-union dataset already contains the rolled-up connection information.

2. **RM and TPS Sales Pro population**

   * Start with clients belonging to the specified Relationship Managers and TPS Sales Pros.
   * Use:

     * RM column: `[RM_COLUMN]`
     * TPS Sales Pro column: `[TPS_SALES_PRO_COLUMN]`
   * The list of relevant RM/TPS names will be provided separately.

3. **Geography classification**
   Use the Data Foundations client-level geography/cross-border field:

   * US
   * Canada
   * US–Canada Cross-border

   Column: `[US_CANADA_CROSS_BORDER_COLUMN]`

   This classification is at the parent-client level.

4. **Market / LOB stratification**
   Use the current market/LOB fields to ensure the required client groups are represented, including groups such as:

   * CF / CFD
   * EMM
   * MM
   * other required US/Canada market groups

   Columns:

   * `[LOB_LEVEL_2_COLUMN]`
   * `[LOB_LEVEL_3_COLUMN]`
   * `[MARKET_COLUMN]`

   Use the actual data values in the canonical dataset rather than colloquial market names.

5. **Regional representation**
   Ensure the final sample has representation across the required US and Canadian regions.

   Region column: `[REGION_COLUMN]`

   Try to distribute the sample reasonably/equally across the required regions where the available population allows it.

6. **Calculate three separate data-coverage metrics**

   For every parent client, calculate coverage separately for:

   **A. Lending / Credit Coverage**
   Use the lending/credit canonical tables:
   `[LIST LENDING TABLES]`

   **B. Deposit Coverage**
   Use the deposit canonical tables:
   `[LIST DEPOSIT TABLES]`

   **C. TPS / Payments Coverage**
   Use the TPS/payments/billing/product canonical tables:
   `[LIST TPS TABLES]`

   For each subject area, calculate a client-level coverage/fill-rate percentage.

   The intent is to know how much usable data exists for each client in:

   * Lending
   * Deposits
   * TPS/Payments

7. **Create a relationship-type proxy**
   Based on the three coverage measures, assign a sampling classification such as:

   * `Lending Heavy`
   * `TPS Heavy`
   * `Deposit Heavy` if useful
   * `Mixed / Lending + TPS`

   This is only a sampling proxy. Do not treat this as an official business classification.

   A client with strong coverage in both lending and TPS should be classified as Mixed.

8. **Fill-rate requirement**
   Prefer clients with at least approximately **65% usable coverage**.

   However, do NOT remove an entire required market/group just because no clients meet 65%.

   If a required segment has no clients above 65%:

   * select the best available candidates,
   * flag them as exceptions,
   * show their actual coverage,
   * and do not silently exclude the segment.

9. **Additional sampling dimensions**
   After satisfying the mandatory geography, market, region, RM/TPS, and relationship-type requirements, use the following dimensions to improve diversity of the sample:

   * Revenue band
   * Profitability band
   * Connection band
   * Complexity band
   * Fill-rate band
   * Industry band
   * Country band

   Existing columns:
   `[REVENUE_BAND_COLUMN]`
   `[PROFITABILITY_BAND_COLUMN]`
   `[CONNECTION_COLUMN]`
   `[COMPLEXITY_COLUMN]`
   `[FILL_RATE_COLUMN]`
   `[INDUSTRY_COLUMN]`
   `[COUNTRY_COLUMN]`

   Within each required pool, prefer meaningful/larger clients with good data coverage rather than very small clients, while maintaining reasonable diversity.

10. **Final sample requirements**
    The final sample should collectively cover as many of these dimensions as possible:

    * US clients
    * Canada clients
    * Cross-border clients
    * Lending-heavy clients
    * TPS-heavy clients
    * Mixed lending + TPS clients
    * Required market/LOB groups
    * Required US regions
    * Required Canadian regions
    * Multiple RMs
    * Multiple TPS Sales Pros
    * Good overall data coverage
    * Diversity in revenue, profitability, complexity, connections and industry

11. **Important implementation rules**

    * Use the latest canonical data I provide.
    * Do not use outdated source tables from Isaac’s original script when an equivalent canonical table is available.
    * Use the current/latest parent-client classification.
    * Do not recreate connection rollups if they already exist in canonical/client-union data.
    * Do not let the USD/CLT column issue block this sampling. Use the values currently available in the Dataiku/canonical data.
    * Keep all sampling logic transparent and easy to review.

12. **Required final output**
    Produce one parent-client-level dataframe containing, at minimum:

    * Parent Client ID
    * Parent Client Name
    * US / Canada / Cross-border classification
    * Current LOB
    * Current Market
    * Region
    * RM Name
    * TPS Sales Pro Name
    * Lending Coverage %
    * Deposit Coverage %
    * TPS/Payments Coverage %
    * Relationship Type / Sampling Proxy
    * Overall Fill Rate
    * Revenue Band
    * Profitability Band
    * Complexity Band
    * Connection Band
    * Industry
    * Country
    * Selected for Sample: Yes/No
    * Selection Reason
    * Exception/Warning

13. **Before writing the final code**
    First inspect the datasets and columns I provided and give me a mapping like:

    `Sampling criterion -> dataset -> column(s) -> transformation/filter logic`

    Do not guess column names.

    If a required field already exists in canonical data, use it directly instead of deriving it again.

Once the mapping is complete, update the existing sampling code and return:

1. the complete updated script,
2. a brief explanation of each sampling step,
3. the final selected-client dataframe,
4. a summary showing counts by US/Canada/Cross-border, market, region, RM/TPS and relationship type,
5. any criteria that could not be fully satisfied because of insufficient available clients.
